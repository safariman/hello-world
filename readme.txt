hoping to manage roots hunt sources
  use git to watch the progress

commit using git aliases
  ga       git add
  gst      git status
  gcmsg    git commit -m     # add commit message in 
                               double OR single quotes
  ga       git add [fileName]
  gd       git diff          # get the change from last commit
